CREATE TABLE Student (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    class INT,
    section VARCHAR(10),
    dob DATE,
    address VARCHAR(500),
    mobile VARCHAR(12)
);


CREATE TABLE User (
    id INT PRIMARY KEY AUTO_INCREMENT,
    fname VARCHAR(100),
    lname VARCHAR(100),
    pwd VARCHAR(10)
);
